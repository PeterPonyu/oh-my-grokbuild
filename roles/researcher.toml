# Researcher — read-only, official docs and time-sensitive facts.
description = "External research role that checks official docs, separates confirmed facts from inference, and cites sources with dates."
default_capability_mode = "read-only"
reasoning_effort = "medium"
default_fork_context = true
