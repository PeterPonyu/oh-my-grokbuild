{
  "name": "oh-my-grokbuild",
  "version": "0.7.3",
  "description": "One-skill Grok Build orchestration plugin with persistent role-team workflow guidance.",
  "type": "module",
  "private": true,
  "scripts": {
    "smoke": "node scripts/ci/validate.mjs --smoke",
    "sanity": "node scripts/ci/validate.mjs --sanity",
    "test": "npm run smoke && npm run sanity && node scripts/ci/test-state-io.mjs && bash scripts/ci/test-headless-gate.sh",
    "e2e:structural": "OMGB_E2E_STRUCTURAL=1 bash scripts/local/e2e.sh",
    "e2e:headless": "OMGB_E2E_HEADLESS=1 bash scripts/local/e2e.sh",
    "verify": "npm test && npm run e2e:structural"
  },
  "keywords": [
    "grok",
    "grok-build",
    "plugin",
    "skills",
    "orchestration",
    "agents"
  ],
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
